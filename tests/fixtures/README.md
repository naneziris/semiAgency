Create these two files by hand on the corporate machine (see docs/d2p/README.md, one-time setup):
- mini.pptx   — 3 slides on the corporate template; shapes named {{title}}, {{body}}, {{subtitle}}
- tracker.xlsx — header row per docs/d2p/design.md §7, two sample rows, formatted as a Table named "Actions"
