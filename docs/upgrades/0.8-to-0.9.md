# Upgrade 0.8 → 0.9: one command after a meeting, current engagement, fewer commands per step

Nothing in existing `engagements/` changes. What does:

- **`scripts/new_engagement.py` now ships** (it needs no corporate fixture). Run it with no arguments: it asks kind, subject, name, stakeholders, audience, minutes, waits while you fill `inputs/`, checks what you put there and prints the prompt to paste. If Copilot already built a `new_engagement.py` for you on 0.7/0.8, replace it with the shipped one.
- **`engagements/CURRENT`** — written by `new_engagement.py`; every script and every prompt falls back to it, so `python scripts/status.py` and `/options` need no engagement name. `--use <name>` switches.
- **`docs/after-meeting.md`** — the one page to follow; README's "Which runbook?" section is replaced by it. Runbooks stay as the per-kind detail.
- **`scripts/append_tasks.py`** shows the rows it appends and offers to pass G5.
- **Prompts**: first line changed to fall back to `CURRENT`; `engagements/${input:engagement}` in the bodies became `<dir>`. Overwrite `.github/` with the 0.9 copy.
- **Scripts to update via Copilot** if they already exist (one prompt, `@analyst`):

```
Read docs/bootstrap.md §2 (the paragraph above the table, and the rows for status.py, build_deck.py, ingest.py, followup_agenda.py, lint_deck.py, timing.py, storyboard.py) and scripts/new_engagement.py (resolve_dir, read_current).
1. Every script that takes an engagement dir: make it optional, fall back to engagements/CURRENT the way new_engagement.py does.
2. status.py: `--pass` with no gate name passes the next unpassed gate interactively (checklist, confirm, ask option/storyline when needed); print the next Copilot prompt with engagement=<name> filled in and the next terminal command at the end; `--use <name>`.
3. build_deck.py: run lint_deck.py after a build unless --no-lint; --notes-only runs timing.py first.
Run selftests and python scripts/selftest_all.py.
```
