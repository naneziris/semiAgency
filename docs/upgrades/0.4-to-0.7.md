# Upgrade 0.4 → 0.7: D2P only, runbooks, renamed prompts, CSV tracker

0.5 and 0.6 added (and 0.7 removed) the other workflow candidates — see `docs/parked.md`. If you are on 0.4 with existing engagements, nothing in `engagements/` changes. What does:

- **Prompts renamed** (old → new): `prep-discovery` → `discovery-prep`, `synthesize-discovery` → `discovery-synthesize` (proposal only; deck kind now uses the new `content`), `sketch-options` → `options`, `write-proposal` → `proposal`, `write-storylines` → `storylines`, `outline-deck` → `deck-outline`, `write-speech` → `speech`, `critique-deck` → `critique`, `brief-team` → `team-brief`, `extract-tasks` → `team-tasks`, `summarize-meeting` → `meeting-notes`. Overwrite `.github/` with the 0.7 copy.
- **Docs**: `README.md` (which runbook, setup, gates), `docs/runbooks/{proposal,deck,meeting}.md` replace the old README steps, `scenarios.md` and the bootstrap cheat sheet. `bootstrap.md` no longer carries verbatim config copies.
- **Tracker**: `tracker/actions.csv` (columns in `docs/design.md` §7) replaces the planned `.xlsx`; `scripts/append_tasks.py` ships. If you had an xlsx, export it to CSV with the new header once.
- **Scripts to update via Copilot** (one prompt, `@analyst`):

```
Read docs/bootstrap.md §2 rows for new_engagement.py and status.py, and docs/runbooks/*.md.
1. new_engagement.py: when --kind is omitted, ask the three "Which runbook?" questions from README.md and derive the kind; end by printing the runbook path and its Step 0.
2. status.py: after the gate summary, print the current `## Step N` section of docs/runbooks/<kind>.md verbatim (the step whose gate is the next unpassed one).
3. validate.py: tasks.json per .github/instructions/tasks.instructions.md.
Run selftests.
```
